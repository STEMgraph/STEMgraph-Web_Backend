services:
  stemgraph_api:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    secrets:
      - github_pat
    volumes:
      - data:/data
    restart: unless-stopped

secrets:
  github_pat:
    file: ./secrets/github.pat

volumes:
  data:
